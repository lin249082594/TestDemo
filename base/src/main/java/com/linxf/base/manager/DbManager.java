package com.linxf.base.manager;

import javax.inject.Singleton;

@Singleton
public class DbManager {
//    public static DaoSession getDaoSession(){
//        DaoMaster.DevOpenHelper mHelper = new DaoMaster.DevOpenHelper(MyApplication.getMyApplication(), MyApplication.getMyApplication().getMemberId(), null);
//
//        // 注意：该数据库连接属于 DaoMaster，所以多个 Session 指的是相同的数据库连接。
//        DaoMaster mDaoMaster = new DaoMaster(mHelper.getWritableDatabase());
//        return  mDaoMaster.newSession();
//    }

}
